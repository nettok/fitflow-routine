use axum::Json;
use axum::response::NoContent;

pub async fn assignment_accept() -> NoContent {
    NoContent
}

pub async fn assignment_start() -> NoContent {
    NoContent
}

pub async fn assignment_complete() -> NoContent {
    NoContent
}
